"""
API layer package.

Contains one versioned sub-package (currently only `v1`). Nothing
version-specific belongs directly in this file — see app/api/v1/__init__.py
for the actual router aggregation.
"""
