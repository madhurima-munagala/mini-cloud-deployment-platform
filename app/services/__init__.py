"""
Business logic (services) lives here, kept separate from the thin
routers in app/api/v1/.

Empty in Stage 2 — the health check's logic is simple enough to live
directly in its router. Later stages will add modules here, e.g.
app/services/github_service.py, app/services/deployment_service.py,
app/services/engine_client.py, so routers stay thin and testable.
"""
